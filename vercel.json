{
  "builds": [
    {
      "src": "weatherApp-UI/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "weatherApp-UI/dist" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
